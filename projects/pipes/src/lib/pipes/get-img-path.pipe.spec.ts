import { GetImgPathPipe } from './get-img-path.pipe';

describe('GetImgPathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetImgPathPipe();
    expect(pipe).toBeTruthy();
  });
});
