import { GetPicturePathPipe } from './get-picture-path.pipe';

describe('GetPicturePathPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPicturePathPipe();
    expect(pipe).toBeTruthy();
  });
});
